# Historical RAG Memory Discord Bot - Python dependencies
# Install with:  pip install -r requirements.txt
# Requires Python 3.10+

discord.py>=2.3.0,<3.0.0
google-generativeai>=0.7.0
python-dotenv>=1.0.0
Pillow>=10.0.0
numpy>=1.26.0

# External database driver (Turso / libSQL). Required only when
# TURSO_DATABASE_URL is configured; harmless to install otherwise.
# Provides a sqlite3-compatible embedded-replica connection.
libsql>=0.1.0
